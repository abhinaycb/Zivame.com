# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ZivameSampleApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ZivameSampleApp
	pod 'Firebase/Core'
    pod 'Firebase/Auth'
    pod 'Firebase/Database'
  target 'ZivameSampleAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ZivameSampleAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
